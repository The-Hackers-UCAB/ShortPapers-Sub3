import 'dart:svg';

class Fecha {
  Number dia;
  Number mes;
  Number ano;
  Fecha(this.dia, this.mes, this.ano);
}

class Hora {
  Number hora;
  Number minuto;
  Hora(this.hora, this.minuto);
}
